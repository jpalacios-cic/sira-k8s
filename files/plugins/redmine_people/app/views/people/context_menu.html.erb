<ul>

  <% if @person.present? %>
    <li><%= context_menu_link sprite_icon('edit', l(:button_edit)), {:controller => 'people', :action => 'edit', :id => @person}, :class => 'icon icon-edit', :disabled => !@can[:edit] %></li>
  <% else %>
  <% end %>

</ul>

